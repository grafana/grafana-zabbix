---
'grafana-zabbix': minor
---

feat(problems): redesign the Problems panel table with Grafana theme styling; severity pills, status dots, two-line host/problem cells, tag chips, compact age, restyled expanded row, data links on the problem description, "auto" page size that fits the panel height (default for new panels), and fix the missing panel/datasource logos in dist
