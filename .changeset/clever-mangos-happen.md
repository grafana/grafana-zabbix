---
'grafana-zabbix': minor
---

Add Multi-Metric table query type with multi-capture group extraction and multi-metrics per entity. Group/host/pattern fields use live-suggestion dropdowns, and metric columns can optionally emit their full history as a time-series frame for rendering as a sparkline.
