coverage:
  precision: 2
  round: down
  range: 50...100

  status:
    project:
      default:
        threshold: 5
    patch: off
    changes: off

comment: off

ignore:
  - "dist/test/test-setup/.*"
  - "src/test-setup/.*"
