apiVersion: 1

providers:
  - name: 'Zabbix test dashboards'
    folder: 'zabbix'
    folderUid: ''
    type: file
    allowUiUpdates: false
    updateIntervalSeconds: 60
    options:
      path: /devenv/dashboards
